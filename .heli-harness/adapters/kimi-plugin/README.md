# Heli-Harness Kimi Plugin / Hooks

- `config.toml.example` — drop-in `[[hooks]]` block for Kimi Code CLI
- `hooks/heli-*.mjs` — wrappers over Claude-style shared hooks

Kimi PreToolUse accepts Claude-like `hookSpecificOutput.permissionDecision` deny JSON.
