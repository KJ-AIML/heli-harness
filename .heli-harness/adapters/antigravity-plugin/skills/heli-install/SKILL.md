---
description: Use when installing or updating Heli-Harness in a parent workspace.
---

# Heli Install

Prefer `npx github:KJ-AIML/heli-harness install <workspace-path>`. Fall back to `install.sh` / `install.ps1` when Node is unavailable.
