# Heli-Harness Cursor Plugin

This plugin is indexed by the local Cursor marketplace at the parent adapter folder.

The plugin provides the Heli-Harness governance rule and the `heli-governance`, `heli-install`, and `heli-target` skills. It does not provide runtime hook enforcement or sandboxing.
